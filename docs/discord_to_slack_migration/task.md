# Discord → Slack Bot 変換タスクリスト

## 1. 依存パッケージの変更
- [x] `package.json` を更新
- [/] `npm install` で `@slack/bolt` をインストール

## 2. メインボットファイルの書き換え
- [x] `slack_bot.js` 新規作成（全機能実装済み）

## 3. 設定ファイル・ドキュメントの更新
- [x] `.env.example`, `start_bot.bat`, `SPECIFICATION.md`, `README.md` 完了

## 4. 動作検証
- [ ] `npm install` 成功確認
- [ ] 構文エラーチェック
